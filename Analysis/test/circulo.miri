program graficas;

main(){
    declare
    int iRadio, iX, iY, iW;
    string sColor;
    bool bFill;
    
    read(iRadio);
    read(sColor);
    read(bFill);
    read(iX);
    read(iY);
    read(iW);
    
    circulo(iRadio, sColor, bFill, iX, iY, iW);
    

}
end;